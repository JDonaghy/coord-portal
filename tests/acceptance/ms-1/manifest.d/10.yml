# ms-1 issue #10 — manifest fragment, owned solely by this issue (#2543).
#
# JIT slice extension, 2026-08-22, against contract.md as amended by note 2
# (issue #107, 2026-08-18): `quality-check` is promoted from
# not-customer-actionable to customer-actionable in the pinned vocabulary
# table. The original sealed slice (94bef08) predates that amendment and
# still treated `quality-check` as one of "the four" read-only rollup states.
# This fragment reflects the extended suite:
#
#   - two ids RENAMED to match the amended counts (old ids are gone on
#     purpose and must not be restored — same convention as issue #14's
#     analogous extension, see the shared manifest.yml's comment on that):
#       "only the two customer-actionable states ask the customer for anything"
#         -> "only the three customer-actionable states ask the customer for anything"
#       "the four rollup states share one read-only template"
#         -> "the three rollup states share one read-only template"
#   - two ids ADDED, asserting the amendment's consequences directly:
#       "quality-check now asks the customer to act, per the #107 amendment"
#       "quality-check's actionable screen still hides internal churn"
#
# All other ids are unchanged from the original slice and were re-verified
# green against the current suite as part of this extension.

issues:
  10:
    - "[chromium] ms-1/10-up-mapping.spec.ts › ms-1 issue 10 up-mapping read model › every customer status renders its pinned wording on the detail screen"
    - "[chromium] ms-1/10-up-mapping.spec.ts › ms-1 issue 10 up-mapping read model › no status outside the fixed vocabulary ever reaches the customer"
    - "[chromium] ms-1/10-up-mapping.spec.ts › ms-1 issue 10 up-mapping read model › only the three customer-actionable states ask the customer for anything"
    - "[chromium] ms-1/10-up-mapping.spec.ts › ms-1 issue 10 up-mapping read model › the three rollup states share one read-only template"
    - "[chromium] ms-1/10-up-mapping.spec.ts › ms-1 issue 10 up-mapping read model › internal churn stays hidden inside the rollup states"
    - "[chromium] ms-1/10-up-mapping.spec.ts › ms-1 issue 10 up-mapping read model › quality-check now asks the customer to act, per the #107 amendment"
    - "[chromium] ms-1/10-up-mapping.spec.ts › ms-1 issue 10 up-mapping read model › quality-check's actionable screen still hides internal churn"
    - "[chromium] ms-1/10-up-mapping.spec.ts › ms-1 issue 10 up-mapping read model › the dashboard renders each status with the same wording as its detail"
    - "[chromium] ms-1/10-up-mapping.spec.ts › ms-1 issue 10 up-mapping read model › a submission shows exactly one status at a time"
    - "[chromium] ms-1/10-up-mapping.spec.ts › ms-1 issue 10 up-mapping read model › the portal renders the status it is given and derives none of its own"
    - "[chromium] ms-1/10-up-mapping.spec.ts › ms-1 issue 10 up-mapping read model › On hold surfaces with the pinned wording or not at all"

# Ids OBSERVED failing (step 4, 2026-08-22) against the CURRENT unimplemented
# state — the daemon-side render for `quality-check` has not yet been updated
# to show an approve/request-changes affordance. Every other id in this
# fragment, including the two renamed ones, was measured green in the same
# run (`npm run test:acceptance -- 10-up-mapping`: 10 passed, 1 failed) and is
# deliberately NOT listed here — they are the regression net for this
# extension, not casualties of it.
expected_red:
  10:
    - "[chromium] ms-1/10-up-mapping.spec.ts › ms-1 issue 10 up-mapping read model › quality-check now asks the customer to act, per the #107 amendment"
