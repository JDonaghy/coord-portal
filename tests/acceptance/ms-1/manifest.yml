# ms-1 — sealed acceptance manifest: test id -> issue slice.
#
# Ids are the `web-playwright` driver's node ids, exactly as
# `coord.acceptance_drivers.parse_playwright_json_report` builds them:
#
#   [{project}] {file relative to testDir} › {describe path} › {test title}
#
# They must match character-for-character (including the `›` separators) —
# `coord acceptance run --issue N` filters by exact id, and an id that matches
# nothing is reported as a missing test, not as a pass.
#
# One block per issue. Later slices MERGE into this file; do not rewrite
# another issue's block.

issues:
  9:
    - "[chromium] ms-1/09-intake.spec.ts › ms-1 issue 9 async intake › the intake screen is a form, not a conversation"
    - "[chromium] ms-1/09-intake.spec.ts › ms-1 issue 9 async intake › submitting the form creates a submission and returns a reference"
    - "[chromium] ms-1/09-intake.spec.ts › ms-1 issue 9 async intake › constraints and project scope really are optional"
    - "[chromium] ms-1/09-intake.spec.ts › ms-1 issue 9 async intake › an empty form creates no submission"
    - "[chromium] ms-1/09-intake.spec.ts › ms-1 issue 9 async intake › each submission gets its own reference"
    - "[chromium] ms-1/09-intake.spec.ts › ms-1 issue 9 async intake › a submission is a durable record, not a live session"
    - "[chromium] ms-1/09-intake.spec.ts › ms-1 issue 9 async intake › a fresh submission shows the customer no design round"
    - "[chromium] ms-1/09-intake.spec.ts › ms-1 issue 9 async intake › no engineer-side identifier reaches the intake surface"
  15:
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › an unauthenticated request is 401 on every bridge route"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › a customer Access identity is not a bridge credential"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › a 401 says nothing about what was wrong"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › a pull returns the customer-authored events after the cursor"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › replaying the same cursor returns the same events"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › limit pages the stream without losing or reordering events"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › every event carries the pinned envelope"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › the same push applied twice yields applied then already_applied"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › a stale revision is already_applied, not an error"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › a push touching a portal-owned field is rejected and applies nothing"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › a rejected update does not advance the revision watermark"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › coord-owned fields are never rejected for ownership"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › a batch returns one result per update, in request order"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › a coord-owned write produces no event"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › a heartbeat is accepted and repeatable"
    - "[chromium] ms-1/15-sync-bridge.spec.ts › ms-1 issue 15 sync bridge › the bridge exposes no inbound registration path"
