# ms-4 — sealed acceptance manifest: test id -> issue slice.
#
# Ids are the `web-playwright` driver's node ids, exactly as
# `coord.acceptance_drivers.parse_playwright_json_report` builds them:
#
#   [{project}] {file relative to testDir} › {describe path} › {test title}
#
# `testDir` is `./tests/acceptance` (see `playwright.acceptance.config.ts`), so
# the file component is `ms-4/<file>.spec.ts`. Ids must match
# character-for-character, including the `›` separators and the em-dashes inside
# a couple of the titles — `coord acceptance run --issue N` filters by exact id,
# and an id that matches nothing is reported as a missing test, not as a pass.
#
# One block per issue. Later slices MERGE into this file; do not rewrite another
# issue's block. Issue #132 is still to be authored.

issues:
  128:
    # RED at authoring time — the migration does not exist yet.
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › the migration lands, and the running Worker reports the newer schema"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › a clients table exists at schema head"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › clients carries exactly the columns issue 128 pins, with the nullability it pins"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › no two clients can share an email address"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › clients is indexed by email"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › projects carries a nullable client_id, and keeps everything 0012 gave it"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › projects is indexed by client_id"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › no foreign key constraint is introduced on either table"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › no backfill — a project created outside lead promotion has a NULL client_id and keeps its email"
    # GREEN at authoring time, and deliberately so — a ratchet and a control.
    # They guard the destructive and the collateral-damage readings of #128
    # respectively, so they must keep passing rather than start passing. Per the
    # observed-not-intended rule they are absent from `expected_red` below.
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › no backfill — a submission keeps the bare customer_email it was filed with"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › the migration disturbs nothing that already reads projects and submissions"

  129:
    # RED at authoring time — promotion knows nothing about `clients` yet, so
    # neither the pre-promotion match card nor the post-promotion attachment
    # sentence renders at all.
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › promoting an unrecognised email says a new client was created"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › an unrecognised email gets a clients row with the email and nothing else"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › the promoted screen shows where the attached submission is"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › a returning address is recognised before the operator commits"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › promoting into a matched client says the work joined that client"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › the operator can start a new project for a client who already has one"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › the operator's chosen project is the one the submission lands in"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › a double-submitted promotion creates one submission and one client"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › a client match ignores the case of the email address"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › a project that never came from a lead promotion is not offered"
    # GREEN at authoring time, and deliberately so — a CONTROL. It guards the
    # over-eager reading of #129 (a match card that renders for a total
    # stranger), so it must keep passing rather than start passing. Per the
    # observed-not-intended rule it is absent from `expected_red` below.
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › a lead from an address no client has ever used shows no match card"
  130:
    # RED at authoring time — none of #130's reassignment surface exists yet.
    #
    # NOTE ON THE LAST THREE. They need a client with TWO projects, and the only
    # black-box way to create one is #129's `client-project-option-new` radio on
    # the promotion form (contract § "Before promotion — client match"). So they
    # currently fail *inside that instrument*, before reaching anything #130
    # owns, and they cannot go green until #129 has landed as well. That is a
    # real dependency of the issue ("needs the `clients` table landed first",
    # and reassignment is meaningless for a client with one project), not a
    # defect in #130's implementation — read their failure message, which says
    # so explicitly, before treating them as #130's problem.
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › a promoted lead offers reassignment, closed by default"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › the disclosure opens and closes from the keyboard-reachable toggle"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › a client with a single project is offered nothing but a new one"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › the panel offers no way to move a submission to a different client"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › only this client's own client-linked projects are on offer"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › moving to another project takes the submission out of the one it was in"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › creating a new project inline moves the submission into it, and reassignment stays available"
    # GREEN at authoring time, and deliberately so — two controls and a ratchet.
    # They guard the "panel rendered where it does not belong", "operator gate
    # forgotten" and "panel pushed ms-2's content off the screen" readings of
    # #130, so they must keep passing rather than start passing. Per the
    # observed-not-intended rule they are absent from `expected_red` below.
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › an unpromoted lead offers no reassignment at all"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › a stranger cannot reassign anything"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › adding reassignment disturbs nothing ms-2 already pins on a promoted lead"

  131:
    # RED at authoring time — neither GET nor POST /account exists yet, so every
    # test that drives the page fails on `account-form` (or `nav-account`) being
    # absent, in a few seconds each, with a message naming the hook.
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › the profile page renders behind the caller's own Access identity"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › a customer with nothing on file yet gets an empty, usable form"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › the profile is reachable from every authenticated customer screen"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › a client can put their own phone, cc emails and address on file"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › editing a profile that already exists replaces what was there"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › saving redirects back to the profile page rather than rendering a dead end"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › the sign-in address cannot be edited from this form"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › one client's profile is never another's"
    # GREEN at authoring time, and deliberately so — a control and a ratchet.
    # They guard the "new route forgot to resolve an identity" and "the new nav
    # entry displaced an existing one" readings of #131, so they must keep
    # passing rather than start passing. Per the observed-not-intended rule they
    # are absent from `expected_red` below.
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › a caller with no Access identity is served no profile material"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › adding the profile entry disturbs nothing on the existing customer topbar"

# Tests OBSERVED failing against the unimplemented state, recorded per issue at
# the moment the slice was authored.
#
# #128 recorded from a full `npm run test:acceptance -- ms-4` run on
# 2026-08-21 against schema head 0015 (`0015_preview_reviews.sql`): 9 failed,
# 2 passed. The nine below are exactly the nine that were watched to fail — the
# two that passed are the ratchet and the control noted above and are excluded
# because they never failed, not because they are out of scope.
#
# #129 recorded from a full `npm run test:acceptance -- ms-4` run on 2026-08-21
# against schema head 0016 (`0016_clients.sql` — #128 had landed by then): 10
# failed, 12 passed. The ten below are exactly the ten that were watched to
# fail. The eleventh test in the slice ("a lead from an address no client has
# ever used shows no match card") passed and is excluded because it never
# failed — it is the control described in its own block above, not an
# out-of-scope test.
expected_red:
  129:
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › promoting an unrecognised email says a new client was created"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › an unrecognised email gets a clients row with the email and nothing else"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › the promoted screen shows where the attached submission is"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › a returning address is recognised before the operator commits"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › promoting into a matched client says the work joined that client"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › the operator can start a new project for a client who already has one"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › the operator's chosen project is the one the submission lands in"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › a double-submitted promotion creates one submission and one client"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › a client match ignores the case of the email address"
    - "[chromium] ms-4/129-lead-promotion-client-link.spec.ts › ms-4 issue 129 lead promotion links a client › a project that never came from a lead promotion is not offered"
  128:
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › the migration lands, and the running Worker reports the newer schema"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › a clients table exists at schema head"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › clients carries exactly the columns issue 128 pins, with the nullability it pins"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › no two clients can share an email address"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › clients is indexed by email"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › projects carries a nullable client_id, and keeps everything 0012 gave it"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › projects is indexed by client_id"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › no foreign key constraint is introduced on either table"
    - "[chromium] ms-4/128-clients-schema.spec.ts › ms-4 issue 128 clients table and projects.client_id › no backfill — a project created outside lead promotion has a NULL client_id and keeps its email"

  # #131 recorded from `npm run test:acceptance -- ms-4/131-account-profile` on
  # 2026-08-21, against a checkout where none of #129/#130/#131/#132 had landed:
  # 8 failed, 2 passed, 47s wall clock. Every failure was a missing-hook message
  # in ~5s (`account-form`, or `nav-account` on the shared customer topbar), not
  # a timeout and not a seeding error — this slice files no leads and no
  # submissions, so it is not exposed to #32's unpinned rate limit the way #130's
  # is. The eight below are exactly the eight watched to fail; the two that
  # passed are the control and the ratchet, excluded because they never failed,
  # not because they are out of scope.
  131:
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › the profile page renders behind the caller's own Access identity"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › a customer with nothing on file yet gets an empty, usable form"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › the profile is reachable from every authenticated customer screen"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › a client can put their own phone, cc emails and address on file"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › editing a profile that already exists replaces what was there"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › saving redirects back to the profile page rather than rendering a dead end"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › the sign-in address cannot be edited from this form"
    - "[chromium] ms-4/131-account-profile.spec.ts › ms-4 issue 131 client self-service profile › one client's profile is never another's"

  # #130 recorded from a full `npm run test:acceptance -- ms-4` run on
  # 2026-08-21, against a checkout where #128's migration (`0016_clients.sql`)
  # HAD already landed and #129/#130/#131/#132 had not: 7 failed, 14 passed.
  # (128's eleven were all green on that run, which is why its block above was
  # recorded differently — it predates that migration existing.)
  #
  # The seven below are exactly the seven watched to fail. The three that passed
  # are the two controls and the ratchet, excluded because they never failed —
  # not because they are out of scope.
  #
  # All seven failed on a missing hook with a readable message in 5-10s, not on
  # a timeout and not on seeding: four on `reassign-open-button` /
  # `reassign-toggle` being absent from the promoted-lead screen, three inside
  # the two-project setup on #129's `client-project-option-new` being absent
  # from the promotion form (see the note in the `issues:` block above).
  130:
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › a promoted lead offers reassignment, closed by default"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › the disclosure opens and closes from the keyboard-reachable toggle"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › a client with a single project is offered nothing but a new one"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › the panel offers no way to move a submission to a different client"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › only this client's own client-linked projects are on offer"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › moving to another project takes the submission out of the one it was in"
    - "[chromium] ms-4/130-reassign-project.spec.ts › ms-4 issue 130 reassign a submission to a different project › creating a new project inline moves the submission into it, and reassignment stays available"
