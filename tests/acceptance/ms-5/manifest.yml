# ms-5 — sealed acceptance manifest: test id -> issue slice.
#
# Ids are the `web-playwright` driver's node ids, exactly as
# `coord.acceptance_drivers.parse_playwright_json_report` builds them:
#
#   [{project}] {file relative to testDir} › {describe path} › {test title}
#
# `testDir` is `./tests/acceptance` (see `playwright.acceptance.config.ts`), so
# the file component is `ms-5/<file>.spec.ts`. Ids must match
# character-for-character, including the `›` separators and the em-dashes
# inside a few titles — `coord acceptance run --issue N` filters by exact id,
# and an id that matches nothing is reported as a missing test, not as a pass.
#
# One block per issue. Later slices MERGE into this file; do not rewrite
# another issue's block.

issues:
  161:
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › the migration lands, and the running Worker reports the newer schema"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › inbound_emails carries the columns issue 161's own schema section names, with the nullability it pins"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › message_id + to_email is unique, and no foreign key constraint is introduced"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › a plain message produces exactly one inbound_emails row with the parsed fields, and touches no routing, lead, or outbox row"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › auth_result records the DMARC verdict from Authentication-Results — pass"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › auth_result records the DMARC verdict from Authentication-Results — fail"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › auth_result records the DMARC verdict from Authentication-Results — none (header absent)"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › loop suppression — Auto-Submitted: auto-replied — never earns a reply"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › loop suppression — Precedence: bulk — never earns a reply"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › loop suppression — List-Unsubscribe present — never earns a reply"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › loop suppression — empty envelope sender (a bounce) — never earns a reply"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › loop suppression — sender is one of this portal's own sending domains — never earns a reply"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › a redelivery of the same Message-ID produces no second row"
    - "[chromium] ms-5/161-inbound-seam.spec.ts › ms-5 issue 161 the inbound seam › an oversized body is stored truncated and flagged, never dropped silently"
  162:
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › the migration lands, and the running Worker reports the newer schema"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › outbox carries approval_state / approved_at / approved_by, not_required by default"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › approval_state is a fixed four-value vocabulary — the CHECK is enforced, not decorative"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › outbox.email_type admits 'intake-reply' without it silently vanishing from GET /outbox"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › a pending row is never claimed by the drain, however many ticks run"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › flipping a pending row to approved sends it on the very next tick"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › a rejected row is never sent and never retried, however many ticks run"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › every pre-existing row (approval_state = not_required) still sends exactly as before"
  163:
    - "[chromium] ms-5/163-inbound-router.spec.ts › ms-5 issue 163 the inbound router › rung 1 — a plus-addressed envelope recipient beats a contradictory sender identity"
    - "[chromium] ms-5/163-inbound-router.spec.ts › ms-5 issue 163 the inbound router › rung 2 — a LEAD-XXXXXX reference quoted in the body routes to that lead"
    - "[chromium] ms-5/163-inbound-router.spec.ts › ms-5 issue 163 the inbound router › rung 2 — a SUB-XXXXXX reference quoted in the subject line also routes there"
    - "[chromium] ms-5/163-inbound-router.spec.ts › ms-5 issue 163 the inbound router › rung 3 — a cc_emails match resolves a single-project known client"
    - "[chromium] ms-5/163-inbound-router.spec.ts › ms-5 issue 163 the inbound router › rung 4 — a two-project client where one is awaiting sign-off wins over a merely more recent one"
    - "[chromium] ms-5/163-inbound-router.spec.ts › ms-5 issue 163 the inbound router › rung 4 → 6 — an exact tie between two equally-weighted projects falls to unrouted, never guessed"
    - "[chromium] ms-5/163-inbound-router.spec.ts › ms-5 issue 163 the inbound router › rung 5 — a historical customer_email match with no clients row resolves via address, then scores"
    - "[chromium] ms-5/163-inbound-router.spec.ts › ms-5 issue 163 the inbound router › a DMARC-fail message from a known client's address falls to unrouted rather than matching them"
    - "[chromium] ms-5/163-inbound-router.spec.ts › ms-5 issue 163 the inbound router › rung 6 — nobody we know at all falls to the default, with no runner-up to speak of"
  164:
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › a stranger's inbound email creates exactly one leads row, via the same createLead() /start uses"
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › a stranger's email with no display name leaves the lead's name null, same as /start's optional field"
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › the lead a stranger's email produces is promotable through the exact same button, and becomes a real submission"
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › the inbound_emails row records both which lead it produced and which draft it produced"
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › the stranger's message drafts exactly one outbox row at pending, never sent however many drain ticks run"
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › re-delivering the same message produces no second lead and no second draft"
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › the drafted acknowledgement never quotes the sender's own message, discloses no state, and carries the lead's own reference"

# Tests OBSERVED failing against the unimplemented state, recorded per issue at
# the moment the slice was authored.
#
# #161 recorded from `npm run test:acceptance -- ms-5/161` (after `npm ci` +
# `npx playwright install chromium`), against a checkout at schema head 0019
# (`0019_client_merge.sql` — nothing in ms-5 had landed yet): 14 failed, 0
# passed, 0 skipped. The fourteen below are exactly the fourteen ids that were
# watched to fail — there is no control or ratchet clause in this slice (every
# test depends on `POST /__email` and/or `migrations/0020_inbound_emails.sql`
# existing, and #161 is the issue that creates both), so nothing is excluded.
#
# #162 recorded from `npm run test:acceptance -- ms-5/162` (after `npm ci` +
# `npx playwright install chromium`), against a checkout at schema head 0020
# (`0020_inbound_emails.sql`, issue #161 — already landed; #162's own
# `migrations/0021_outbox_approval.sql` had not): 8 failed, 0 passed, 0
# skipped. All eight ids below were OBSERVED failing — every test in this
# slice depends on `migrations/0021_outbox_approval.sql` (the
# `approval_state`/`approved_at`/`approved_by` columns and the widened
# `email_type` CHECK) and/or `src/drain.ts`'s new clause existing, and #162 is
# the issue that adds both, so nothing is excluded as a control or ratchet
# clause.
#
# #163 recorded from `npm run test:acceptance -- ms-5/163` against a checkout
# with #161 and #162 already landed (migrations through
# 0021_outbox_approval.sql) but #163 itself unimplemented (no
# src/inboundRouter.ts, nothing wiring it into inbound processing): 9 failed,
# 0 passed, 0 skipped. All nine ids below are exactly the nine OBSERVED
# failing, every one on the same signal — `inbound_emails.routed_rung` read
# back as `null` where the test expected a specific rung — because #163 is
# the issue that fills it in. There is no control or ratchet clause in this
# slice.
#
# #164 recorded from `npm run test:acceptance -- ms-5/164` (after `npm ci` +
# `npx playwright install chromium`), against a checkout with #161/#162/#163
# already landed (schema head 0021_outbox_approval.sql) but #164 itself
# unimplemented (no createLead()/enqueue wiring for the rung-6 stranger case
# in src/inboundEmail.ts, src/leads.ts or src/notifications.ts): 7 failed, 0
# passed, 0 skipped. Every one of the seven ids below was watched to fail,
# all on the same underlying signal — `inbound_emails.routed_lead_id` (and,
# downstream, `.outbox_id`) read back `NULL` where each test expected a real
# row — because #164 is the issue that fills them in. There is no control or
# ratchet clause in this slice to exclude: every test's very first assertion
# past `POST /__email` succeeding depends on #164's own write path existing.
expected_red:
  162:
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › the migration lands, and the running Worker reports the newer schema"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › outbox carries approval_state / approved_at / approved_by, not_required by default"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › approval_state is a fixed four-value vocabulary — the CHECK is enforced, not decorative"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › outbox.email_type admits 'intake-reply' without it silently vanishing from GET /outbox"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › a pending row is never claimed by the drain, however many ticks run"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › flipping a pending row to approved sends it on the very next tick"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › a rejected row is never sent and never retried, however many ticks run"
    - "[chromium] ms-5/162-outbox-approval.spec.ts › ms-5 issue 162 outbox approval state › every pre-existing row (approval_state = not_required) still sends exactly as before"
  164:
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › a stranger's inbound email creates exactly one leads row, via the same createLead() /start uses"
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › a stranger's email with no display name leaves the lead's name null, same as /start's optional field"
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › the lead a stranger's email produces is promotable through the exact same button, and becomes a real submission"
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › the inbound_emails row records both which lead it produced and which draft it produced"
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › the stranger's message drafts exactly one outbox row at pending, never sent however many drain ticks run"
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › re-delivering the same message produces no second lead and no second draft"
    - "[chromium] ms-5/164-em4-stranger-lead.spec.ts › ms-5 issue 164 EM-4: a stranger's email becomes a lead › the drafted acknowledgement never quotes the sender's own message, discloses no state, and carries the lead's own reference"
