# ms-5 — sealed acceptance manifest fragment for issue #167 (#2543: this file
# belongs to #167 alone; no other slice writes to it).
#
# Ids are the `web-playwright` driver's node ids, exactly as
# `coord.acceptance_drivers.parse_playwright_json_report` builds them:
#
#   [{project}] {file relative to testDir} › {describe path} › {test title}
#
# `testDir` is `./tests/acceptance` (see `playwright.acceptance.config.ts`), so
# the file component is `ms-5/167-em7-promote-inbound.spec.ts`. Ids must match
# character-for-character, including the `›` separators and the em-dashes
# inside a few titles.

issues:
  167:
    - "[chromium] ms-5/167-em7-promote-inbound.spec.ts › ms-5 issue 167 EM-7: promote an inbound email to a submission, in one click › promoting a matched reply creates exactly one submission in the matched project, owned by the sender, and leaves EM-5's own thread message untouched"
    - "[chromium] ms-5/167-em7-promote-inbound.spec.ts › ms-5 issue 167 EM-7: promote an inbound email to a submission, in one click › promoting an inbound email produces exactly one submission.created event, shaped like an ordinary intake"
    - "[chromium] ms-5/167-em7-promote-inbound.spec.ts › ms-5 issue 167 EM-7: promote an inbound email to a submission, in one click › promoting the same inbound email twice — a retry and two concurrent attempts — converges on one submission"
    - "[chromium] ms-5/167-em7-promote-inbound.spec.ts › ms-5 issue 167 EM-7: promote an inbound email to a submission, in one click › \"Change route\" to an operator-chosen project, then promote, lands the new submission in that chosen project"
    - "[chromium] ms-5/167-em7-promote-inbound.spec.ts › ms-5 issue 167 EM-7: promote an inbound email to a submission, in one click › a stranger's inbound email (already a lead) has no promote path here, and a direct POST creates no submission"
    - "[chromium] ms-5/167-em7-promote-inbound.spec.ts › ms-5 issue 167 EM-7: promote an inbound email to a submission, in one click › a discarded draft cannot be promoted afterward — the pending guard blocks it"

# Observed from the step-4 run against the current (unimplemented) state —
# `POST /replies/:id/promote` 404s, since #167 has not landed on this branch.
# The two omitted ids (the stranger/no-project row, and the already-discarded
# row) already pass today: both assert a guard invariant ("no submission is
# created") that holds trivially while the route does not exist at all, so
# they are not listed here per the OBSERVED-not-INTENDED rule.
expected_red:
  167:
    - "[chromium] ms-5/167-em7-promote-inbound.spec.ts › ms-5 issue 167 EM-7: promote an inbound email to a submission, in one click › promoting a matched reply creates exactly one submission in the matched project, owned by the sender, and leaves EM-5's own thread message untouched"
    - "[chromium] ms-5/167-em7-promote-inbound.spec.ts › ms-5 issue 167 EM-7: promote an inbound email to a submission, in one click › promoting an inbound email produces exactly one submission.created event, shaped like an ordinary intake"
    - "[chromium] ms-5/167-em7-promote-inbound.spec.ts › ms-5 issue 167 EM-7: promote an inbound email to a submission, in one click › promoting the same inbound email twice — a retry and two concurrent attempts — converges on one submission"
    - "[chromium] ms-5/167-em7-promote-inbound.spec.ts › ms-5 issue 167 EM-7: promote an inbound email to a submission, in one click › \"Change route\" to an operator-chosen project, then promote, lands the new submission in that chosen project"
