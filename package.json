{
  "name": "coord-portal",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Customer intake and design sign-off portal for a claude-coordinator fleet",
  "license": "MIT",
  "scripts": {
    "db:migrate:local": "wrangler d1 migrations apply coord-portal --local",
    "db:migrate:remote": "wrangler d1 migrations apply coord-portal --remote",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "serve:acceptance": "rm -rf ./.wrangler/state && wrangler d1 migrations apply coord-portal --local && wrangler dev --port 8789 --test-scheduled --var MAIL_PROVIDER:fake --var EMAIL_FROM:'coord-portal <notify@intake.heurontech.com>' --var PUBLIC_BASE_URL:http://127.0.0.1:8789",
    "serve:test": "wrangler d1 migrations apply coord-portal --local && wrangler dev --port 8788 --test-scheduled --var MAIL_PROVIDER:fake --var EMAIL_FROM:'coord-portal <notify@intake.heurontech.com>' --var PUBLIC_BASE_URL:http://127.0.0.1:8788",
    "test": "vitest run",
    "test:acceptance": "playwright test --config=playwright.acceptance.config.ts",
    "test:e2e": "playwright test",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.e2e.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0",
    "yaml": "^2.9.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "workerd@1.20260801.1": true
  }
}
